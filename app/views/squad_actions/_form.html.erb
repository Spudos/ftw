<%= form_with(model: squad_action) do |form| %>
  <% if squad_action.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(squad_action.errors.count, "error") %> prohibited this squad_action from being saved:</h2>

      <ul>
        <% squad_action.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
