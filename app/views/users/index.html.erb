<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="col-md-6">
      <h3>Hey <%= current_user.fname %></h3><div>
      <br>
      <br>
      <h5>Your Account Details</h5>
      <p>First Name: <%= current_user.fname %></p>
      <p>Last Name: <%= current_user.lname %></p>
      <p>Email: <%= current_user.email %></p>
      <a <%= link_to "Edit these details", edit_user_registration_path, class: "btn btn-sm btn-primary" %></a>
      <br>
      <br>
      <% if current_user.club >= 1%>
        <div>
        <p>You are currently the manager of <strong><%= Club.find_by(id: current_user.club)&.name %></strong>
        who play in <strong><%= Club.find_by(id: current_user.club)&.league %></strong></p>
        <a <%= link_to "Resign from club", users_resign_path, class: "btn btn-sm btn-danger" %></a>
        </div>
      <% else %>
        <div>
        <p>You are not currently managing a club</p>
        <a <%= link_to "Manage a club", help_club_creation_path, class: "btn btn-sm btn-success" %></a>
        </div>
      <% end%>
      <br>
      <p>If you would like us to remove your account please raise an issue from our help page</p>
      <a <%= link_to "Raise an issue", new_feedback_path, class: "btn btn-sm btn-primary" %></a>
  </div>
</div>